Add two 16 - bit hexadecimal numbers stored in registers and store the result in data segment

data segment
res dd 0
data ends

code segment
assume cs:code,ds:data
start:
mov ax, data
mov ds, ax
mov ax,1111h
mov bx,2222h
adc ax,bx
mov ds,ax

mov ah,4ch
int 21h

code ends
end start
