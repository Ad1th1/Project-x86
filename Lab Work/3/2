Add two 8 - bit decimal numbers stored in data segment and store the result in data segment

data segment
res dd 0
data ends

code segment
assume cs:code,ds:data
start:
mov ax, data
mov ds, ax
mov al,11h
mov bl,22h
adc al,bl
mov ds,ax

mov ah,4ch
int 21h

code ends
end start
